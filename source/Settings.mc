class Settings {
    var type;
    var duration;
    var power;
    var heartRate;

    function initialize(type, duration, power, heartRate) {
        self.type = type;
        self.duration = duration;
        self.power = power;
        self.heartRate = heartRate;
    }
}